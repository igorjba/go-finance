package main

// "../go-test/api"
// "../go-test/config"

func main() {
	// config.Init()

	// app := fiber.New()

	// app.Use(logger.New())

	// api.SetupRoutes(app)

	// port := os.Getenv("PORT")
	// if port == "" {
	// 	port = "3000"
	// }

	// log.Fatal(app.Listen(":" + port))
}
